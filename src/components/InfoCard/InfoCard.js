import './InfoCard.css'

export default function InfoCard( props ) {
    return(
        <div className='info-card-container' style={ {backgroundColor: `${ props.backgroundColor }`} }>
            
        </div>
    )
}